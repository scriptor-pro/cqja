---
layout: layouts/base.njk
title: Ce Que J'ai Appris
description: Notes personnelles publiees en continu, triees par themes et dates.
pagination:
  data: collections.homePages
  size: 1
  alias: homePage
permalink: "{% if pagination.pageNumber == 0 %}/{% else %}/page/{{ pagination.pageNumber + 1 }}/index.html{% endif %}"
---

<h1>Ce Que J'ai Appris</h1>
<p class="lede">Notes courtes, classées, reliées par thèmes.</p>

<section class="theme-terminal-home">
  <div class="terminal-bar" aria-hidden="true">
    <span class="dot"></span>
    <span class="dot"></span>
    <span class="dot"></span>
    <span>carnet terminal / serif + sans-serif</span>
  </div>

  <div class="terminal-shell">
    <div class="home-grid terminal-grid">
      <section class="panel terminal-panel">
        <p class="terminal-label">journal / page {{ homePage.pageNumber }} sur {{ homePage.totalPages }}</p>
        <ul class="home-notes terminal-notes">
        {% for note in homePage.notes %}
          <li class="note-item terminal-note-item">
            <a class="terminal-title" href="{{ note.url }}">{{ note.data.title }}</a>
            <div class="note-content">{{ note.templateContent | safe }}</div>
            <small class="terminal-meta">{{ note.date | dateFr }}</small>
            {% if note.data.tags and note.data.tags.length > 0 %}
            <ul class="note-tags" aria-label="Tags de la note">
              {% for tag in note.data.tags %}
                {% if tag != "note" %}
                <li><a href="/tags/{{ tag | tagSlug }}/">#{{ tag }}</a></li>
                {% endif %}
              {% endfor %}
            </ul>
            {% endif %}
          </li>
        {% endfor %}
        </ul>

        {% set current = homePage.pageNumber %}
        {% set total = homePage.totalPages %}
        <nav class="home-pagination" aria-label="Pagination accueil">
          <div>
            {% if current > 1 %}
              {% if current - 1 == 1 %}
              <a href="/">&larr; Précédente</a>
              {% else %}
              <a href="/page/{{ current - 1 }}/">&larr; Précédente</a>
              {% endif %}
            {% endif %}
          </div>
          <div class="home-pagination-current">Page {{ current }} / {{ total }}</div>
          <div>
            {% if current < total %}
            <a href="/page/{{ current + 1 }}/">Suivante &rarr;</a>
            {% endif %}
          </div>
        </nav>
      </section>

      <aside class="home-tags panel terminal-panel">
        <p class="terminal-label">index des thèmes</p>
        <ul class="tag-cloud">
        {% for item in collections.tagStats %}
          {% if loop.index <= 12 %}
          <li>
            <a href="/tags/{{ item.tag | tagSlug }}/">{{ item.tag }} ({{ item.count }})</a>
          </li>
          {% endif %}
        {% endfor %}
        </ul>
        <p><a href="/tags/">Voir tous les tags</a></p>
      </aside>
    </div>
  </div>
</section>
